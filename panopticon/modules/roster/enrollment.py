"""ROSTER — Phase 4 : reconnaissance locale opt-in.

L'enrôlement (extraction d'empreinte à partir des photos de référence,
avec consentement obligatoire) est implémenté dans ``api/routes/persons.py``
(endpoint ``POST /api/persons``), qui s'appuie sur ``modules.roster.face_engine``.
"""
